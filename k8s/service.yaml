apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: gke-terra
spec:
  type: LoadBalancer
  selector:
    app: myApp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http